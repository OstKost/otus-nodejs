meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  additionalCommands: npm run build
  artifacts:
    "dist/*": /dist

run:
  command: npm run start:prod
  containerPort: 3000